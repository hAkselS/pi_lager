'''
File:   packetize_dive_results.py

Spec:   This script compiles basic information about the most recent deployment
        including the results from the FKW_detector into a single text file.

I/O:    This script reads from the config.yamnl. This script also looks into 
        FKW_detector/dive_logs to find the most recent detector results (when applicable)

Usage:  <python3 packet_handling/packetize_drive_results.py>

ID:     pdr
'''

print("pdr: I have been called (that's all for now folks)")